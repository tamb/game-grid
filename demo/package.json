{
  "name": "@tamb/gamegrid-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Parcel demo consuming the sibling @tamb/gamegrid package",
  "scripts": {
    "clean": "node prerun/cleanup.js",
    "compile": "node prerun/compile.js",
    "start": "npm run compile && npx parcel src/output.html --no-cache --dist-dir dist --open",
    "build": "npm run compile && npx parcel build src/output.html --dist-dir dist --public-url ./ --no-cache"
  },
  "license": "ISC",
  "dependencies": {
    "@tamb/gamegrid": "file:.."
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.16.4",
    "handlebars": "^4.7.8",
    "parcel": "^2.16.4"
  }
}
