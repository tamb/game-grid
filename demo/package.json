{
  "name": "y",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node prerun/prerun.js && npm install && npx parcel src/output.html --no-cache --dist-dir dist --open"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gamegrid": "./../gamegrid-1.0.0-beta.3.tgz"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.11.0",
    "handlebars": "^4.7.8",
    "parcel": "^2.11.0",
    "rimraf": "^5.0.5"
  }
}