{
  "name": "y",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node prerun/cleanup && npm install && node prerun/compile && npx parcel src/output.html --no-cache --dist-dir dist --open"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gamegrid": "./../tamb-gamegrid-1.0.0-beta.16.tgz"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.16.4",
    "handlebars": "^4.7.8",
    "parcel": "^2.16.4",
    "rimraf": "^6.1.3"
  }
}
